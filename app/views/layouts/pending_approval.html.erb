<!DOCTYPE html>
<html lang="<%= I18n.locale %>" class="overflow-x-hidden">
  <head>
    <title><%= content_for(:title) || "TSM System" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="description" content="Transport Management System">
    <meta name="theme-color" content="#2563EB">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.svg">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= tag.meta name: "turbo-refresh-method", content: "morph" %>
  </head>

  <body class="bg-gray-50 font-sans min-h-screen overflow-x-hidden">
    <main class="min-h-screen flex items-center justify-center p-4">
      <%= yield %>
    </main>
    <%= render "shared/flash" %>
  </body>
</html>
